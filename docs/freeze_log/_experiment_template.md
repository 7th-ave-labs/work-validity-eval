# Experiment: YYYY-MM-DD_short_slug

## Purpose

What question does this run answer (e.g. task-only vs title+task embeddings, new prescreen)?

## Commands (exact)

```bash
# paste full CLI with flags
```

## Inputs

- O*NET release / download script:
- Prepared tasks: `data/derived/onet_tasks_prepared.parquet` (notes):
- Embeddings: `data/derived/embeddings/...`
- Clustering: `scripts/cluster_onet_tasks.py` params:

## Outputs (paths)

- Clusters / figures:
- Cluster review CSV:
- Coded tasks (if any):

## Notes

- API model ids / snapshot ids:
- Anything that did not reproduce bit-for-bit:
